PORT=3001
NODE_ENV=development
CLIENT_URL=http://localhost:3000
INVITE_PAGE_URL=http://localhost:3000/invitation

JWT_ACCESS_SECRET=strong_access_secret
JWT_REFRESH_SECRET=strong_refresh_secret
JWT_INVITE_SECRET=strong_invite_secret

ACCESS_TOKEN_TTL=15m
REFRESH_TOKEN_TTL=7d
INVITE_TOKEN_TTL=7d
REFRESH_COOKIE_MAX_AGE_MS=604800000

# PostgreSQL
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/trello_clone
PGHOST=localhost
PGPORT=5432
PGUSER=postgres
PGPASSWORD=postgres
PGDATABASE=trello_clone
PGSSL=false

# SMTP email delivery
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=test@gmail.com
SMTP_PASS=app_pass
SMTP_FROM=test@gmail.com

# Set true to log emails instead of sending (default true in development)
SMTP_MOCK=false
